import { DevStep } from "../../components"
import { DefaultProps } from "../../types"

export const Ajuda: React.FC<DefaultProps> = ({ setCurrentPage }) => {
    return (            
        <DevStep/>   
    )
}